# Container image build
OPENCODE_VERSION=latest

# Web access. Replace both values before exposing through Nginx.
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=CHANGE_ME_LONG_RANDOM_PASSWORD
OPENCODE_PORT=4096
# Публичный URL второго интерфейса; используется только как ссылка из кабинета.
OPENCODE_PUBLIC_URL=http://127.0.0.1:4096

# Кабинет руководителя и его PostgreSQL. Оба пароля генерирует make init.
CONTROL_PLANE_SERVER_USERNAME=manager
CONTROL_PLANE_SERVER_PASSWORD=CHANGE_ME_MANAGER_PASSWORD
CONTROL_PLANE_PORT=8088
CONTROL_PLANE_DB_PASSWORD=CHANGE_ME_DATABASE_PASSWORD
CONTROL_PLANE_DEFAULT_MONTHLY_BUDGET=25000

# Срок хранения локальных резервных копий.
BACKUP_RETENTION_DAYS=14

# Active key route: shared or separate.
KEY_MODE=shared

# Temporary common gateway mode (AITunnel).
AITUNNEL_API_KEY=

# Direct-provider mode. Fill only before switching to separate.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
# Compatibility alias used by some Google SDK versions.
GEMINI_API_KEY=

# Optional GitHub access for private repositories. Prefer a fine-grained token.
GH_TOKEN=

# Торговые API намеренно не подключены к AI Orchestra. Ключи брокеров и бирж
# должны находиться только в отдельном execution-контуре.

# Git identity used only inside the isolated container.
GIT_AUTHOR_NAME=AI Development Department
GIT_AUTHOR_EMAIL=ai-department@local
GIT_COMMITTER_NAME=AI Development Department
GIT_COMMITTER_EMAIL=ai-department@local
